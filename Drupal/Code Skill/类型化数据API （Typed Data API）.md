#### `概述`
```
创建类型化数据 API 是为了向开发人员提供以不同方式与数据交互的一致方式。

与许多其他语言不同，PHP 是一种非常松散的类型语言。它没有明确定义它处理的不同类型的数据。
因此，Drupal曾经是一样的。例如，没有一致的方法来判断字段值是字符串、整数还是时间戳。
甚至，判断某些内容是否可翻译或可访问（如权限）。

需要有一种一致的方式来编辑数据或描述数据。
```

