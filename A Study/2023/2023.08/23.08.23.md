# Entity API

```php
每个实体都是特定“实体类型”的实例。
某些内容实体类型具有子类型，称为“捆绑包”，而对于其他实体类型，只有一个捆绑包。

例如，用于 Drupal 中主要内容页面的 Node 内容实体类型具有称为“内容类型”的捆绑包，而用于用户帐户的用户内容类型只有一个捆绑包。
```

```php
播放列表

一个用户有多个列表
用户可以对列表CRUD

一个列表有多个视频
用户可以对video CRUD

列表
用户id、列表id、视频id、列表名称、视频数量
UID PID Pname VID
```

